* «chapter_one_introduction» -название новой локальной ветки которую мы пушим во внешний репозиторий
```
user@PC:~/playbooks$ git push
fatal: Текущая ветка chapter_one_introduction не имеет вышестоящей ветки.
Чтобы отправить текущую ветку и установить внешнюю ветку как вышестоящую для этой ветки, используйте

    git push --set-upstream origin chapter_one_introduction

user@PC:~/playbooks$ git push --set-upstream origin chapter_one_introduction
```